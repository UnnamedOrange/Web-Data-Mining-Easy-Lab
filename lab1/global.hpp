#pragma once

#include <filesystem>
#include <string>
#include <vector>

namespace global
{
    inline std::filesystem::path working_path;
    inline std::vector<std::string> index_list;
} // namespace global
